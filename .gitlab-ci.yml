# The Docker image that will be used to build your app
image: filesforfreedom

before_script:
  - npm i -g firebase-tools

prod-hosting:
  stage: deploy
  script:
    - firebase deploy --token $FIREBASE_TOKEN
  environment:
    name: production
    url: https://files-for-freedom.web.app/
